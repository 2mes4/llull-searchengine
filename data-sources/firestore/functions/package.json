{
  "name": "muntaner-search-sync",
  "version": "0.1.0",
  "description": "Firebase Extension: Syncs Firestore documents to Muntaner search engine",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "test": "npm run build && jest"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": "22"
  }
}
